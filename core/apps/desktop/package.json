{
  "name": "ctx-desktop",
  "private": true,
  "version": "0.66.0",
  "license": "GPL-3.0-or-later",
  "scripts": {
    "ensure-dev-resources": "bash scripts/ensure-dev-resources.sh",
    "dev": "pnpm run ensure-dev-resources && pnpm exec tauri dev",
    "build": "pnpm run ensure-dev-resources && pnpm exec tauri build"
  },
  "devDependencies": {
    "@crabnebula/tauri-driver": "2.0.8",
    "@crabnebula/test-runner-backend": "0.1.18",
    "@tauri-apps/cli": "^2.9.6",
    "@wdio/cli": "^9.8.1",
    "@wdio/local-runner": "^9.8.1",
    "@wdio/mocha-framework": "^9.8.1",
    "@wdio/runner": "^9.8.1",
    "@wdio/spec-reporter": "^9.8.1",
    "webdriverio": "^9.8.1"
  }
}
